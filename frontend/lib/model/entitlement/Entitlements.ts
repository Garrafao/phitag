class ENTITLEMENTS {
    private static readonly PREFIX = 'ENTITLEMENT'; 
    public static readonly ADMIN = `${ENTITLEMENTS.PREFIX}_ADMIN`; 
    public static readonly USER = `${ENTITLEMENTS.PREFIX}_USER`; 
}

export default ENTITLEMENTS;